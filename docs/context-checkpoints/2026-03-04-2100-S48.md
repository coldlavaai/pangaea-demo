# Context Checkpoint — 2026-03-04 21:00 (S48)

## Commits This Session (S48)
```
5f39d45  feat: ALF Notifications Bot — inline keyboard buttons
d0e11c3  fix: users-panel updates instantly after drawer save/delete
4cd3972  fix: deleteUser nullifies all 16 FK references
d37484c  feat: user edit drawer — click user to edit name, phone, Telegram ID
c9774bf  fix: NCR query uses resolved=false not status column
bf36b49  fix: cast labour_requests query as any in telegram-notify webhook
3918756  fix: cast NCR query as any in telegram-notify webhook
78b3828  feat: @AlfNotificationsBot — new admin notification bot with reply handler
641b01b  feat: activity feed expansion — WhatsApp, docs + offer events (S48)
```

## What Was Done This Session (S48)

### Activity Feed Expansion
- Sophie intake complete → `application_complete` notification (push: true)
- ID doc uploaded → `document_uploaded` (push: false, warning if AI flagged)
- CSCS card uploaded → `cscs_uploaded` (push: false)
- Offer accepted → `offer_accepted` (push: true)
- Offer declined → `offer_declined` (push: true, severity: warning)
- ActivityFeed: grouped filter tabs (All/Telegram/Applications/Documents/Offers/Compliance)
- New type icons for all event types including cron types

### @AlfNotificationsBot (NEW — separate from @AztecSiteBot)
- Token: `8782817037:AAHmSSk4hRZVQPffLUsMAisAf0_1TVcV3f8` (env: TELEGRAM_NOTIFY_TOKEN)
- Webhook: `https://aztec-landscapes-bos.vercel.app/api/webhooks/telegram-notify`
- `src/lib/telegram/send-notify.ts` — sendNotifyBot() + answerCallback()
- `src/app/api/webhooks/telegram-notify/route.ts` — full handler
- **Inline keyboard buttons**: 🔔 Unread | 📋 Recent | ⚠️ NCRs | 🏗️ Requests | 📊 Status | ✅ Mark all read
- Handles both text commands AND callback_query (button taps)
- Commands: recent, unread, ncrs, requests, status, markread
- Verifies sender by telegram_chat_id → finds user row

### User Edit Drawer (NEW)
- Click any user name in Settings → right-side drawer slides in
- Edit: first name, last name, phone, Telegram Chat ID (numeric)
- Email shown read-only
- Delete user button (handles all 16 FK constraints + deletes Supabase Auth)
- Instant optimistic update — no page refresh needed
- `src/components/settings/user-edit-drawer.tsx`
- `updateUserDetails` + improved `deleteUser` server actions

### Notifications: switched to @AlfNotificationsBot
- `createNotification()` now uses `sendNotifyBot()` instead of `sendTelegram()`
- DM messages include a link to the BOS item (APP_URL + link_url)
- Type union expanded to include all 17 notification types

### Settings: Telegram linked indicator
- Shows "Telegram linked" (sky blue, Send icon) under phone if telegram_chat_id set
- Settings page query now includes telegram_chat_id

### deleteUser: FK handling fixed
- Nullifies all 16 FK references in parallel before delete
- Tables: documents, labour_requests, allocations, timesheets (x3), performance_reviews, non_conformance_incidents (x2), ncr_comments, audit_log, alerts, advert_templates, adverts, operatives, message_threads, user_sites

## Current DB State
| User | telegram_chat_id | receive_notifications | is_active |
|------|---|----|--|
| liam.butt@aztec-landscapes.co.uk | null | false | true |
| donna@thefoundersfriend.co.uk | null | false | true |
| jacob@coldlava.ai | null | false | true |
| oliver@coldlava.ai | 1640953016 | true | true |

Jacob Stray (oliver@OTDM.net) — deleted via SQL

## NEXT TASKS (Priority Order)

### 1. INVITE EMAIL — Outlook/Microsoft OAuth (NOT STARTED)
Oliver wants to use Microsoft OAuth (Graph API) to send emails — same pattern as Solar BOS.
- Replace Resend with Microsoft Graph API sendMail
- Use client credentials flow (no user login needed)
- Configure Supabase custom SMTP with same Microsoft account
- All emails (invites, notifications) come from official Aztec email
- Solar BOS has this built — check `/Users/oliver/Documents/internal/bos/CL-SOLARBOS-JAN26` for pattern
- Env vars needed: MICROSOFT_TENANT_ID, MICROSOFT_CLIENT_ID, MICROSOFT_CLIENT_SECRET, EMAIL_FROM
- Current invite code: `src/app/(dashboard)/settings/actions.ts` → `inviteUser()` (generates link, no email sent)

### 2. INVITE EMAIL — Welcome/Onboarding Email Content (NOT STARTED)
When a user is invited, they need a well-designed email with:
- **For all roles**: login link, BOS URL, credentials
- **For site managers**: how to use @AztecSiteBot on Telegram (step by step)
- **For admins/staff**: how to use @AlfNotificationsBot, how to link Telegram
- Role-specific instructions, properly branded
- "Extensive and well-designed" per Oliver

### 3. @AztecSiteBot — JJ needs re-inviting as site manager
- JJ (Jacob) needs new user row + invite email
- Once row exists, he can verify with @AztecSiteBot
- Hold until email sending is working

### 4. NOTIFICATION TESTING
- Oliver to do WhatsApp operative signup → should get application_complete push
- JJ to sign up as site manager → should get a notification (there isn't one for this yet — needs adding)
- Oliver to test @AlfNotificationsBot after clearing chat (should say "Hi Oliver!")

### 5. Admin user names
- liam.butt, donna, jacob@coldlava.ai all have first_name='Admin', last_name='User'
- Use new edit drawer to fix proper names

### 6. Existing pending tasks
- Remove 🔄 Reset from @AztecSiteBot MAIN_MENU once testing done
- QA pass: mobile, Sophie E2E, allocation race condition
- Presentation gaps: equipment cert gating, location matching, Gov.uk RTW API, 2FA

## Key File Locations (S48 additions)
| Purpose | Path |
|---|---|
| Notify bot send util | `src/lib/telegram/send-notify.ts` |
| Notify bot webhook | `src/app/api/webhooks/telegram-notify/route.ts` |
| Notification helper | `src/lib/notifications/create.ts` |
| User edit drawer | `src/components/settings/user-edit-drawer.tsx` |
| Settings actions (updated) | `src/app/(dashboard)/settings/actions.ts` |
| Activity feed component | `src/components/activity/activity-feed.tsx` |

## Architecture Notes
- @AztecSiteBot = site manager commands (TELEGRAM_BOT_TOKEN)
- @AlfNotificationsBot = admin DM push + query bot (TELEGRAM_NOTIFY_TOKEN)
- telegram_chat_id is UNIQUE constraint on users table
- receive_notifications = true → gets DM push on push:true notifications
- createNotification() is non-fatal (wrapped in try/catch)
- All new DB columns not in TS types → use `as any`
