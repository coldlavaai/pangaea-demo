# Context Checkpoint — 2026-03-04 17:00 (S45)

## Commits This Session (S45)
```
58896f4  feat: NCR flow — type and severity selection via inline buttons
2581e31  feat: telegram bot — date confirmation on arrive + search for large rosters
3805d29  fix: allocations.actual_start column name (not actual_start_date)
e8da321  feat: reports — add NCR summary and RAP performance sections
aacd049  feat: email invites via Resend
8ee259a  feat: timesheet PDF export
1ec09d7  feat: terminate allocation from operative detail page
2e1668f  fix: remove onCreated prop — can't pass functions server→client in App Router
5f37d3b  feat: quick-assign allocation on operative detail page
```

## What Was Done This Session (S45)

### Operative Detail — Allocations Tab
- **Add allocation** button: `QuickAssignAllocation` component — site dropdown, day rate, start date. Server action `quickAssignAllocation` in `src/app/(dashboard)/operatives/actions.ts`. Checks operative status + RTW before inserting (labour_request_id = null).
- **End allocation** button: `TerminateAllocationButton` component — two-click confirm, sets status=terminated + end_date=today. Only shows on pending/confirmed/active rows.
- **Bug fixed**: Can't pass `() => {}` function from Server Component to Client Component in Next.js App Router — silently breaks render. Fixed by removing onCreated prop, using router.refresh() internally.

### Timesheet PDF Export
- `@react-pdf/renderer` was already installed (v4.3.2)
- New: `src/components/timesheets/timesheet-pdf.tsx` — A4 PDF, ALF branding, operative details (name, ref, UTR, NI), entries table, gross pay summary, approval status
- New: `src/app/api/timesheets/[id]/pdf/route.ts` — GET route, nodejs runtime, auth-gated
- "Download PDF" button added to `TimesheetActions` alongside existing "Export CSV"

### Email Invites via Resend
- `resend` package installed (v6.9.3)
- New: `src/lib/email/send.ts` — `sendInviteEmail()` function, branded HTML email
- Site managers get Telegram bot setup instructions in the email
- Silently skips if `RESEND_API_KEY` env var not set (no breakage)
- Hooked into `src/app/(dashboard)/settings/actions.ts` `inviteUser()` — fires after link generated
- **TO ACTIVATE**: Oliver needs to create free Resend account → get API key → `vercel env add RESEND_API_KEY production`

### Reports Page — New Sections
- **NCR Summary**: total/unresolved/critical counts, type breakdown pills, unresolved incidents table
- **RAP Performance**: green/amber/red traffic light counts, per-operative average score table with progress bar, sorted lowest-first
- NCR severity enum is `minor | major | critical` (NOT high/medium/low)

### Telegram Bot — Arrive Flow Improvements
- **Date confirmation**: after selecting operative, bot asks "Arriving today (DD/MM)?" with inline buttons "✅ Today" or "📅 Different date". If different → type DD/MM or DD/MM/YYYY
- **Search for large rosters**: if >6 operatives, bot asks to type name/ref instead of showing all buttons. Filters in memory, shows top 6 matches.
- **Trade category** added to operative button labels: `AZT-0010 · Oliver Tatler · Groundworker`
- **Bug fixed**: column was `actual_start` not `actual_start_date` — arrive action was silently failing to set it

### Telegram Bot — NCR Flow Improvements
- After description, now shows **incident type** selection (9 types in 2-column grid)
- Then shows **severity** selection: ⚪ Minor | 🟠 Major | 🔴 Critical
- Then operative selection (search if >6)
- Both type and severity saved to DB; confirmation message shows all details
- New callback actions: `ncr_type:TYPE`, `ncr_sev:SEVERITY`

## Test Status (Telegram Bot)
| Flow | Status |
|---|---|
| Verification (email link) | ✅ Working — Jacob Stray verified |
| RAP scoring | ✅ Tested — Jacob submitted 5/5 on Oliver Tatler (3 Mar) |
| Mark Arrived | ✅ Working (Jacob confirmed) — allocation moved to active |
| NCR — description only | ✅ Tested and worked (type/severity were hardcoded) |
| NCR — with type+severity | ❌ Not yet tested — just deployed, needs Jacob to retest |
| Date picker on arrive | ❌ Not yet tested |

## Current Database State
- Oliver Tatler (AZT-0010, id: bb127d2c-60b7-4421-a25e-6f0c70f0cdbc)
  - 1 allocation to JJ Test Site (ad6f3a2d-25a6-4ae5-8e24-e23040eefb02)
  - Allocation status: likely 'active' after arrive test
  - 1 RAP review: A:5 R:5 P:5, avg 5.0, green, by Jacob Stray
- Jacob Stray: site manager, telegram_chat_id linked, verified ✅

## Telegram Group Chat (Liam's question)
- **Current**: bot is DM-only (private chats only — filtered in webhook route)
- **Liam's question**: should bot be in team group chat?
- **Decision**: Liam creates normal Telegram group himself (no Cold Lava involvement needed). Bot stays DM-only.
- **Future option agreed**: bot can post broadcast notifications INTO the group when actions happen (arrive/NCR/RAP). Not built yet. Needs TELEGRAM_GROUP_ID env var + `sendTelegram(groupId, msg)` calls after each action.

## Key File Locations (S45 additions)
| Purpose | Path |
|---|---|
| Operative page server actions | `src/app/(dashboard)/operatives/actions.ts` |
| Quick-assign component | `src/components/operatives/quick-assign-allocation.tsx` |
| Terminate allocation button | `src/components/operatives/terminate-allocation-button.tsx` |
| Timesheet PDF component | `src/components/timesheets/timesheet-pdf.tsx` |
| Timesheet PDF route | `src/app/api/timesheets/[id]/pdf/route.ts` |
| Email send utility | `src/lib/email/send.ts` |
| Telegram handler (full rewrite) | `src/lib/telegram/site-manager-handler.ts` |

## Remaining Work (ordered priority)
1. **NCR retest with type+severity** — ask Jacob to log another NCR, verify in BOS
2. **Arrive date picker test** — ask Jacob to test "Different date" option
3. **Telegram group broadcast** — add bot to group, post notifications on arrive/NCR/RAP (TELEGRAM_GROUP_ID env var)
4. **Resend API key** — Oliver to create account + add key to Vercel
5. **QA pass** — mobile responsiveness, Sophie E2E, allocation offer race condition
6. **Presentation gaps still outstanding**:
   - Equipment certification gating (CPCS/NPORS/LANTRA)
   - Location-based offer matching (distance calc)
   - Gov.uk RTW verification API
   - 2FA (Supabase TOTP)

## Architecture Reminders (carry forward)
- `createServiceClient()` = bypasses RLS, use for server actions
- `createClient()` = user auth, respects RLS
- `allocation_status` enum: `pending | confirmed | active | completed | terminated | no_show`
- `ncr_severity` enum: `minor | major | critical`
- Next.js App Router: CANNOT pass regular functions as props from Server → Client Components
- FK join syntax: `table!fk_name(cols)` — required for ambiguous joins
- Git email for Vercel auto-deploy: must be `otatler@gmail.com`
