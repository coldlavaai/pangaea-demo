# Context Checkpoint — 2026-03-04 21:00 (S47)

## Commits This Session (S47)
```
a11fcc4  feat: BOS notification system + Telegram labour flows (S47)
8de2dc7  fix: settings users panel — sites inline in user cell, bell toggle visible
9b7fa03  fix: cast notifications query result to unknown first to bypass missing link_url type
8038fca  feat: activity feed page + fix notification bell z-index
```

## What Was Done This Session (S47)

### Notification System — Full Build
- **`src/lib/notifications/create.ts`** (NEW): `createNotification()` — inserts into `notifications` table + sends Telegram DM push to all users with `receive_notifications = true` AND `telegram_chat_id IS NOT NULL`
- **Push tiers**: Arrivals + RAP = BOS bell only (no spam) · NCR/Labour Request/Finish = bell + Telegram DM push
- **Settings > Users**: Bell/BellOff toggle per user row (amber = ON, slate = OFF)
- **`toggleReceiveNotifications`** server action in `src/app/(dashboard)/settings/actions.ts`
- **AlertsBell improvements**: `link_url` click-through, Supabase Realtime live badge (INSERT subscription), `z-[9999]` z-index fix, "View all activity →" link
- **RealtimeRefresh**: Added `notifications` table subscription

### New Telegram Flows
- **🏗️ Request Labour**: trade selection (inline buttons) → headcount (1-5 buttons + text fallback) → start date (Tomorrow / Next Monday / text DD/MM) → inserts into `labour_requests` table → notification with `push: true`
- **🔚 Finish Operative**: shows only `active` operatives → confirm last day (today or custom date) → sets allocation `status = 'completed'` + `actual_end_date` → notification with `push: true`
- **MAIN_MENU updated**: `['📍 Mark Arrived', '⚠️ Log NCR'], ['⭐ Rate Operative', '🏗️ Request Labour'], ['🔚 Finish Operative', '❓ Help'], ['🔄 Reset']`

### New States + Callbacks in site-manager-handler.ts
- States: `sm_labour_headcount`, `sm_labour_start`, `sm_finish_search`, `sm_finish_confirm`, `sm_finish_custom_date`
- Callbacks: `labour_trade`, `labour_hc`, `labour_tomorrow`, `labour_next_monday`, `finish_sel`, `finish_now`, `finish_date`
- New helpers: `startLabourRequestFlow()`, `submitLabourRequest()`, `startFinishFlow()`, `finishOperative()`, `getActiveOperativesForSite()`, `searchActiveOperatives()`

### Notification Triggers Added to Existing Flows
- **After markArrived()**: type=arrive, severity=info, push=false
- **After NCR insert**: type=ncr, severity mapped from ncr_severity, push=true, chains `.select('id').single()` to get NCR ID for link_url
- **After submitRap()**: type=rap, severity=info, push=false

### Activity Feed Page
- **`/activity`** page: full notification feed with type filter tabs (All / Arrived / NCR / RAP Score / Labour Request / Finished)
- **`src/components/activity/activity-feed.tsx`**: colour-coded icons per type, severity left border, live Realtime INSERT subscription, mark-read on click, unread dot indicator
- Added to sidebar under COMMS section, hidden from site_manager role

### Settings Fixes
- Site assignment moved INLINE into user cell (under phone number) — removed ugly separate `<tr>` row
- Bell/BellOff toggle added to Status column for each user

### Migration 00033 (✅ RUN in Supabase)
```sql
ALTER TABLE notifications ADD COLUMN IF NOT EXISTS link_url text;
ALTER TABLE notifications ADD COLUMN IF NOT EXISTS ncr_id uuid REFERENCES non_conformance_incidents(id);
ALTER TABLE users ADD COLUMN IF NOT EXISTS receive_notifications boolean NOT NULL DEFAULT true;
ALTER PUBLICATION supabase_realtime ADD TABLE public.notifications;
```

## Current State — What's Working
| Feature | Status |
|---|---|
| BOS notification bell (live badge, dropdown, click-through) | ✅ |
| Activity feed page (/activity) | ✅ |
| Telegram arrive/NCR/RAP → BOS notification | ✅ |
| Telegram NCR/labour/finish → Telegram DM push | ✅ (needs Liam verified with bot) |
| Settings: receive_notifications toggle | ✅ |
| Request Labour flow (Telegram) | ✅ |
| Finish Operative flow (Telegram) | ✅ |
| All S46 features (NCR detail, Telegram log, etc.) | ✅ |

## NEXT TASK — Activity Feed Expansion (NOT YET BUILT)
Oliver wants `/activity` to be the **helicopter view** of ALL platform events, not just Telegram. Plan is saved at `~/.claude/plans/compressed-sniffing-gosling.md`. Key additions:

### Events to add `createNotification()` calls:
1. **Sophie intake completes** → `src/lib/whatsapp/sophie-handler.ts` line ~344 (`docs_link_sent`)
2. **ID document uploaded** → `src/app/api/apply/[token]/upload/route.ts` line ~409
3. **CSCS card uploaded** → `src/app/api/apply/[token]/upload-cscs/route.ts` line ~154
4. **Offer accepted** → `src/lib/whatsapp/offer-handler.ts` line ~69
5. **Offer declined** → `src/lib/whatsapp/offer-handler.ts` line ~99
6. **Update ActivityFeed** → new type icons + grouped filter tabs (Telegram / Applications / Documents / Offers / Compliance)

### Already creating notifications (cron jobs — just need feed UI icons):
- `compliance_block`, `doc_expiring_30/60/90`, `request_ending`, `missing_timesheet`, `goodwill_doc_reminder`

## Key File Locations (S47 additions)
| Purpose | Path |
|---|---|
| Notification helper | `src/lib/notifications/create.ts` |
| Activity feed page | `src/app/(dashboard)/activity/page.tsx` |
| Activity feed component | `src/components/activity/activity-feed.tsx` |
| AlertsBell (updated) | `src/components/alerts-bell.tsx` |
| RealtimeRefresh (updated) | `src/components/realtime-refresh.tsx` |
| Settings actions (updated) | `src/app/(dashboard)/settings/actions.ts` |
| Settings users panel (updated) | `src/components/settings/users-panel.tsx` |
| Telegram handler (updated) | `src/lib/telegram/site-manager-handler.ts` |
| Telegram send + MAIN_MENU (updated) | `src/lib/telegram/send.ts` |
| Migration 00033 | `docs/migrations/00033_notifications_preferences.sql` |
| Expansion plan | `~/.claude/plans/compressed-sniffing-gosling.md` |

## Architecture Reminders
- `createServiceClient()` = bypasses RLS | `createClient()` = user auth, respects RLS
- `createNotification()` needs a `SupabaseClient` (service client)
- `receive_notifications boolean` on `users` — defaults true — not in TS types (use `as any`)
- `link_url text` and `ncr_id uuid` on `notifications` — not in TS types (use `as any`)
- `telegram_chat_id bigint` on `users` — not in TS types (use `as any`)
- Git email for Vercel deploy: must be `otatler@gmail.com`
- Telegram sessions: `phone_number = 'tg:{chatId}'` in `message_threads`
- `handleCommand()` now has 7th param `chatId: number`

## Remaining Tasks (Priority Order)
1. **Activity feed expansion** — add WhatsApp/doc/offer events (plan saved, next task)
2. **Get Liam verified with Telegram bot** — @AztecSiteBot → verify with email → then DM push works
3. **Remove 🔄 Reset** from MAIN_MENU once testing complete
4. **Resend API key** — Oliver: `vercel env add RESEND_API_KEY production`
5. **QA pass** — mobile, Sophie E2E, allocation race condition
6. **Presentation gaps**: equipment cert gating, location matching, Gov.uk RTW API, 2FA
