# Context Checkpoint — 2026-03-05 01:00 (S49)

## Commits This Session (S49)
```
63b82b5  feat: expanded email editor toolbar + better Telegram instructions
692f4fb  chore: add tiptap dependencies to package.json
c760575  feat: WYSIWYG email template editor with live preview
4d9d0d7  feat: email template editor in Settings
de60c9a  feat: email logging + activity feed Email tab
f68237b  fix: escape apostrophe in integrations panel JSX
a9393c4  chore: trigger redeploy for Microsoft OAuth env vars
bf798e9  fix: use tenant-specific OAuth endpoint when MICROSOFT_TENANT_ID is set
1240453  feat: Microsoft OAuth email — delegated login, integrations settings tab, invite templates
581863b  fix: @AlfNotificationsBot — persistent keyboard, better NCR output, URL buttons
```

---

## What Was Done This Session (S49)

### @AlfNotificationsBot UX Fixes
- Switched from `inline_keyboard` (appended per-message) to `reply_markup.keyboard` (persistent dock)
- `normalizeCmd()` strips emoji from button text for matching
- NCR output now includes operative name (via FK join), incident_type, severity, 80-char excerpt, age
- URL buttons per NCR linking to BOS (`[🔗 View NCR 1](url)`)
- Files: `src/lib/telegram/send-notify.ts`, `src/app/api/webhooks/telegram-notify/route.ts`

### Microsoft OAuth Delegated Email
- Azure app: "Aztec Bot Email" — Client ID `369e4e0d-b854-4c7d-97ae-c9ec2f1b76f8`, Tenant ID `69bb3224-a4d3-45f2-9eaf-8976aa55614a`
- Account type: "My organization only" → tenant-specific OAuth endpoint required
- Files created:
  - `src/lib/email/outlook-auth.ts` — getAuthUrl, exchangeCode, refreshAccessToken, sendGraphMail
  - `src/app/api/integrations/outlook/auth/route.ts` — start OAuth
  - `src/app/api/integrations/outlook/callback/route.ts` — handle callback, store tokens
  - `src/app/api/integrations/outlook/disconnect/route.ts` — disconnect
  - `src/components/settings/integrations-panel.tsx` — connect/disconnect UI
- Vercel env vars added: `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `MICROSOFT_TENANT_ID`
- Oliver connected his Outlook account successfully ✅

### Email Logging + Activity Feed
- `logEmail()` helper in `send.ts` — inserts into `email_log` table
- Activity page queries both `notifications` and `email_log`, merges by `created_at`
- Activity feed: "Email" filter tab, real-time subscription on `email_log`
- `email_sent` items: type='email_sent', read=true (no mark-read needed), severity from status

### Email Template System Architecture
- **Split into two files to avoid server/client import conflicts:**
  - `src/lib/email/template-defs.ts` — client-safe: types, TEMPLATE_DEFINITIONS, renderTemplate, buildTelegramSection, getRoleNote, buildInviteShell
  - `src/lib/email/templates.ts` — server-only: getTemplate() (DB lookup with fallback)
- **Template body_html stores TipTap content only** (not full email HTML)
- **buildInviteShell()** wraps content in full branded email envelope when sending/previewing
- `src/lib/email/send.ts`: sendInviteEmail() gets content from DB/default → renderTemplate() → buildInviteShell() → sendGraphMail()
- Server actions: `saveEmailTemplate()`, `resetEmailTemplate()` in settings/actions.ts

### Email Template Editor (WYSIWYG)
- TipTap extensions: StarterKit, Link, Placeholder, TextAlign, Underline, Color, TextStyle
- **Toolbar:** Undo/Redo | H2/H3 | Bold/Italic/Underline/Strike | Align L/C/R | Colour picker (7 swatches) | Bullet/Ordered/Blockquote | Link/HR
- Split pane: editor left (flex-2), live preview iframe right (flex-3), always visible
- Preview uses `buildInviteShell` with example variable values + site_manager telegram section
- Variable chips: click to insert at TipTap cursor, copy button on hover
- Save/Reset both call `router.refresh()` to sync `isCustomised` badge
- CSS: `.email-editor .ProseMirror` styles in `src/app/globals.css`
- Files: `src/components/settings/email-templates-panel.tsx`

### Invite Email Telegram Section
- Download badges: iOS App Store, Google Play, Desktop — real links
- Blue CTA button: `https://t.me/AztecSiteBot` / `https://t.me/AlfNotificationsBot`
- Site manager: 5-step verification flow (open bot → send email → check email for code → send code → verified)
- Admin/staff: 4-step setup (get Chat ID from `t.me/userinfobot` → BOS profile → enable notifications → open bot)
- `buildTelegramSection()` in template-defs.ts

---

## SQL Migrations Applied This Session
- `email_integrations` table — Oliver ran ✅
- `email_log` table — Oliver ran ✅
- `email_templates` table — ⚠️ **NOT YET RUN** — see PROJECT_STATE.md for SQL

---

## Current Outlook Integration Status
- Liam or Oliver: connected Outlook account via Settings → Integrations ✅
- Tokens stored in `email_integrations` table, auto-refresh 1min before expiry
- Invite emails sending via Microsoft Graph API `POST /v1.0/me/sendMail`

---

## Next Session — Priority Order
See `docs/TODO.md` for full detail. In order:

1. **Oliver runs `email_templates` SQL** (immediate, unblocked)
2. **Fix admin display names** (Liam/Donna/Jacob in Settings → Users)
3. **Short domain** — replace TinyURL in Sophie with `/r/[code]` internal redirect
   - Route: `src/app/r/[code]/route.ts`
   - Helper: `src/lib/short-link.ts` → `createShortLink(url)`
   - Update sophie-handler.ts (search for `tinyurl`)
   - `short_links` table already exists
4. **Dashboard live stats** — active ops, open requests, pending offers, open NCRs, docs awaiting review, missing timesheets
5. **JJ re-invite** — Settings → Users → Invite User (site_manager role)
6. **E2E notification testing** — full chain from WhatsApp → notification → @AlfNotificationsBot DM
7. **Offer expiry notification** — add createNotification() to offer-expiry cron (gap)
8. **Mobile QA pass**
9. **Sophie E2E full test**

---

## Known Bugs / Watch Points
- `short_links` table exists but app still calls TinyURL — need to switch (Priority 3 above)
- Offer expiry cron fires silently — no notification created (Priority 7 above)
- `LIAM_WHATSAPP_NUMBER` env var may be a test number — confirm with Liam before go-live
- Admin display names all show generic (Liam/Donna/Jacob need fixing in Settings UI)
