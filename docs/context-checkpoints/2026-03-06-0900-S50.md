# Context Checkpoint — 2026-03-06 09:00 (S50)

## Commits This Session (S50)
```
44ce253  feat: director briefing page at /briefing (public, no auth)
f866680  docs: director briefing for today's call
19dc59d  fix: comms — hide Telegram threads and orphaned (deleted operative) threads
994878a  feat: dashboard — live NCR/offer/docs stats + notifications feed
```

---

## What Was Done This Session (S50)

### Pre-call demo prep — director call with Aztec

### Dashboard Improvements
- **Added 3 new stat cards** (secondary row): Open NCRs, Pending Offers, Docs Awaiting Review
  - Open NCRs: queries `non_conformance_incidents` where `resolved = false`
  - Pending Offers: queries `allocations` where `status = 'pending'` and `offer_expires_at > now`
  - Docs Awaiting Review: queries `documents` where `status = 'pending'`
- **Fixed Recent Activity panel**: was querying `alerts` table (empty/unused). Now queries `notifications` table with severity-coloured dot, title, body, timestamp
- Added "View all →" link to `/activity`
- Uses `createServiceClient()` for org-level notification/NCR/offer/docs queries
- File: `src/app/(dashboard)/dashboard/page.tsx`

### Comms Page Fix (WhatsApp)
Two issues visible in UI:
1. **`tg:999` and `tg:1640953016`** were appearing — Telegram sessions stored with `phone_number = 'tg:{chatId}'` were leaking into WhatsApp comms list
2. **Orphaned thread** from deleted operative (`+447359821793` / Jacob) was showing — operative deleted but message_thread remained

Fixes:
- Added `.not('phone_number', 'like', 'tg:%')` to DB query — excludes Telegram threads at source
- Added `operative_id` to select, then post-filtered: `!(t.operative_id && !t.operative)` — removes threads where operative was deleted
- File: `src/app/(dashboard)/comms/page.tsx`

### Director Briefing
- Presentation cross-referenced at `https://aztec-presentation.vercel.app/`
- Created `docs/DIRECTOR_BRIEFING.md` — markdown version
- Created `src/app/briefing/page.tsx` — styled public page (no auth required)
- Live at: `https://aztec-landscapes-bos.vercel.app/briefing`
- Sections: What's built | The two bots | Phase 2 | Additions beyond spec | Telegram vs WhatsApp rationale

### Key Discoveries This Session
- **Short domain already built** — `src/lib/whatsapp/shorten.ts` + `src/app/r/[code]/route.ts` both exist and are wired into `sophie-handler.ts`. TODO item was stale.
- **`email_templates` table already exists** — ran migration previously, 42P07 error confirms it
- **`alerts` table** is an old/separate table from `notifications` — dashboard was querying it (empty). Fixed.

### SQL Provided to Oliver
User names update (Oliver to run in Supabase SQL editor):
```sql
UPDATE users SET first_name = 'Liam', last_name = 'Butt'
WHERE email = 'liam.butt@aztec-landscapes.co.uk';

UPDATE users SET first_name = 'Donna', last_name = 'Admin'
WHERE email = 'donna@thefoundersfriend.co.uk';

UPDATE users SET first_name = 'Jacob', last_name = 'Stray'
WHERE email = 'jacob@coldlava.ai';
```
Status: **Unknown** — Oliver was about to run it before laptop restart. Verify in Settings → Users.

---

## Current System Status
- Build: ✅ passing
- All commits pushed to `main`
- Vercel: auto-deployed from all commits above

---

## Next Session — Priority Order

1. **Verify user names SQL ran** — check Settings → Users for Liam Butt / Donna Admin / Jacob Stray
2. **JJ re-invite** — Settings → Users → Invite User → site_manager role → assign sites → JJ receives email
3. **Offer expiry notification** — add `createNotification()` to `src/app/api/v1/cron/offer-expiry/route.ts` (currently silent gap)
4. **E2E notification testing** — full chain: WhatsApp → Sophie intake → operative created → notification → @AlfNotificationsBot DM
5. **Mobile QA pass** — JJ is on mobile 100%
6. **Short domain** — ✅ already done (was marked as TODO but already built)
7. **@AztecSiteBot cleanup** — remove 🔄 Reset button from keyboard once JJ retests

## Presentation Gaps (Phase 2 roadmap — from director briefing)
- Equipment cert gating (block allocation if CPCS/NPORS expired)
- Location matching (distance operative home → site)
- Gov.uk RTW API
- 2FA (Supabase TOTP)
- Rate matrices / day-rate governance
- Reallocation queue (auto-try next operative when offer expires)

---

## Known Bugs / Watch Points
- Offer expiry cron fires silently — no notification created (Priority 3 above)
- `LIAM_WHATSAPP_NUMBER` env var may be a test number — confirm before go-live
- `alerts` table exists in DB but is unused — separate from `notifications`. Don't confuse them.
- If an operative is deleted, their `message_threads` row remains (no CASCADE on FK). Currently filtered in the comms page query — long term should add `ON DELETE CASCADE` to `message_threads.operative_id` FK via SQL migration.
