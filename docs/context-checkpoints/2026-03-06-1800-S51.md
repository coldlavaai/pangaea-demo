# Context Checkpoint — 2026-03-06 18:00 — S51 End

## What Was Done This Session

### Architecture — Multi-trade, Multi-CSCS, Agencies
SQL migrations 00035–00039 all ran successfully:
- `00035` — `import_logs` extended with `skipped_rows jsonb`, `warned_rows jsonb`
- `00036` — `operative_trades` junction table (operative_id, trade_category_id, skill_level, is_primary)
- `00037` — `operative_cscs_cards` table (multiple cards per operative, scheme field)
- `00038` — `agencies` table + `agency_id` FK on operatives + `data_completeness_score` on operatives
- `00039` — `operative_grade` enum extended with 9 new values; 20 new trade_categories inserted

### Import Audit Trail
- `import/confirm/route.ts` — now tracks skipped_rows + warned_rows in import_logs, fires bulk_import notification
- `activity-feed.tsx` — bulk_import type added (violet, Upload icon, 'imports' filter group)
- `/operatives/import/history/page.tsx` — new admin page listing all imports with expandable detail
- `import-history.tsx` — client component: expandable per-import cards, skipped/warned tabs, copy NI/phone

### Importer Updates (`src/lib/import/operative-importer.ts`)
- ParsedRow: `trades[]`, `cscsCards[]`, `agencyName`, `completenessScore` added
- GRADE_MAP: all 9 new grades + common variants (e.g. 'ground worker', 'gw', 'supervisor')
- `parseCscsField()`: parses compound CSCS strings, extracts scheme, returns CscsCard[]
- `splitTradeString()`: splits on / & and, returns string[]
- `data_completeness_score` auto-detected from trailing unnamed column

### Questions Doc
- `docs/QUESTIONS.md` — 30 audited questions, each unblocks specific code/config
- Known Facts section (rate = hourly, plant operator = plant/vehicle operator)
- Q28 option 3 ("delete records") removed — obviously wrong
- `docs/create-liam-questions-form.gs` — Apps Script for Oliver to paste into Google Sheets editor

## State at Checkpoint

- **Import has been run** (Oliver clicked Confirm during this session)
- All 4 S51 commits pushed to remote
- `docs/QUESTIONS.md` updated (Q28 fixed) — NOT YET committed
- Checkpoint file created — NOT YET committed

## Pending — Immediate

1. **Commit + push** QUESTIONS.md fix + this checkpoint
2. **Send 30-question list to Liam** via WhatsApp group
3. **LIAM_WHATSAPP_NUMBER** — update in Vercel (currently Oliver's number)
4. **Resend API key** — `vercel env add RESEND_API_KEY production`

## Pending — Blocked on Liam's Answers

| Blocked Item | Blocking Question |
|---|---|
| `src/lib/pay-rates.ts` — new grade rate bands | Q1 |
| Grade dropdown (one field or two) | Q2 |
| Semi-skilled enum value | Q3 |
| Trade category list finalised | Q4–Q8 |
| Compliance cron — which card schemes valid | Q9 |
| Compliance cron — dual card logic | Q10 |
| Compliance cron — expired = block or warn | Q11 |
| Labour transfer UI | Q12–Q13 |
| Offer approval gate | Q14–Q15 |
| Site manager comms (Telegram vs WhatsApp) | Q16 |
| WhatsApp alert number for Liam | Q17 |
| Telegram group broadcast | Q18 |
| Sophie Stage 1/2 restructure | Q19–Q21 |
| RAP Excel bulk importer | Q22–Q24 |
| Liam's role (admin → staff) | Q25 |
| Donna's role | Q26 |
| Other user accounts | Q27 |
| Invalid NI handling | Q28 |
| Agencies Settings tab | Q29 |
| Sprint prioritisation | Q30 |

## Pending — Not Blocked

- Operative profile: Trades tab (show operative_trades, add/remove)
- Operative profile: CSCS cards tab (show operative_cscs_cards, add/remove)
- Pool search: update to match operative_trades not just trade_category_id
- JJ re-invite (Settings → Users → Invite → site_manager)
- Offer expiry notification gap (cron fires silently, no notification created)
- Remove Reset button from @AztecSiteBot keyboard (after JJ retests)

## Key Technical Reminders

- `rowIdMap: Map<rowIndex, operative_id>` — positional mapping from batch insert
- `createServiceClient()` bypasses RLS — always add `.eq('organization_id', orgId)`
- `allocation_status`: `pending|confirmed|active|completed|terminated|no_show`
- `ncr_severity` enum: `minor|major|critical`
- Telegram sessions: `phone_number = 'tg:{chatId}'` in message_threads
- FK joins: `table!fk_name(cols)` required for ambiguous joins
