# Context Checkpoint — 2026-03-02 16:30 (S42 end)

## Commits This Session (S42)
```
5b3ce11  feat: timesheet operative filter + comms intake_state improvements
c6af8aa  feat: WTD enforcement — cron, shift flags, reports section
f0feddb  feat: use real ALF mark PNG for sidebar icon, login page, and favicon
ba51846  fix: middleware matcher — exclude public image files from auth check
afcd6c4  feat: ALF logo integration + Liam profile fixes
3d0e290  feat: CV parsing + fix cron allocation_status bugs
ab18fcf  feat: comms search — searchable WhatsApp thread with match highlighting
02ac27b  feat: reports page — RTW + CSCS overhaul for ISO compliance
```

## What Was Done This Session (S42)
- Reports overhaul: RTW stats + needs-attention filter, CSCS section ✅
- Comms search: real-time filter + amber highlight ✅
- CV parsing: POST /api/operatives/[id]/parse-cv — Claude extracts work_history + cv_summary ✅
- Cron bug fixes: allocation_status → status column name ✅
- ALF branding: real PNG mark (transparent bg) in sidebar, login page, favicon ✅
- Middleware fix: static PNG/SVG files excluded from auth check ✅
- Liam profile: trading name in PageHeader, EngagementStrip always shown ✅
- Liam/Donna accounts: aztec@coldlava.ai (admin), donna@azteclandscapes.co.uk (staff) ✅
- WTD enforcement: nightly cron sets shift flags, Reports WTD section ✅
- Timesheet filter by operative: search input, server-side ID lookup ✅
- Comms intake_state: specific step labels in thread list + detail header ✅

## Accounts Created
| User | Email | Password | Role |
|---|---|---|---|
| Liam | aztec@coldlava.ai | AztecLiam2026! | admin |
| Donna | donna@azteclandscapes.co.uk | AztecDonna2026! | staff |

## REMAINING BUILD — Ordered Priority

### 🔴 Priority 1: Site Manager WhatsApp Channel (S21/S22)
**What it is:** A WhatsApp bot for site managers (not Sophie — separate state machine).
Site managers text in from site, bot handles:
1. **Arrival confirmation** — "X operatives arrived on site" → updates allocation status
2. **NCR reporting** — site manager describes issue via WhatsApp → creates NCR record, triggers pool search if operative needs replacing
3. **RAP submission** — site manager rates operative via WhatsApp (A/R/P 1-5 each)

**Key files to create:**
- `src/lib/whatsapp/site-manager-handler.ts` — new state machine (separate from sophie-handler.ts)
- Update `src/lib/whatsapp/handler.ts` — route incoming messages to correct handler based on sender type (check if phone matches a site manager in `public.users`)
- `src/app/api/webhooks/twilio/route.ts` — already handles incoming, just needs routing logic

**DB needs:**
- `public.users` already has phone_number? Check — may need to add it
- NCRs table already exists ✅
- Allocations table ✅

**State machine for site manager:**
```
idle → awaiting_ncr_description → awaiting_ncr_operative → done
idle → awaiting_rap_operative → awaiting_rap_attitude → awaiting_rap_reliability → awaiting_rap_punctuality → done
idle → arrival_confirmation → done
```

**Trigger words:**
- "arrived" / "on site" → arrival flow
- "ncr" / "incident" / "problem" → NCR flow
- "rap" / "review" / "rate" → RAP flow
- "help" → menu

---

### 🔴 Priority 2: Public Induction Page (S28)
**What it is:** `/induction/[token]` — a public page operative opens on their phone before first shift.
Contains: company rules, safety info, declaration to sign (checkbox + name).
On completion: marks `allocations.induction_complete = true`, sends confirmation WhatsApp to operative.

**Key files:**
- `src/app/induction/[token]/page.tsx` — public page (no auth)
- `src/app/api/induction/[token]/complete/route.ts` — POST to mark complete
- Migration: add `induction_complete boolean DEFAULT false` + `induction_token uuid` to allocations

**Induction content:** Use the Company_Induction_Form_Rev2.pdf in docs/client/ as source material.

**Flow:** Allocation created → generate induction token → link included in offer WhatsApp → operative opens page → reads + signs → allocation marked induction_complete.

---

### 🟡 Priority 3: Sophie Multi-Language (S20)
**What it is:** Same 7-question intake flow, translated prompts for RO/PL/BG.
Language detection: first message language → set thread.language → all subsequent Sophie messages in that language.

**Key files:**
- `src/lib/whatsapp/sophie-handler.ts` — add language param to all message sends
- Create `src/lib/whatsapp/sophie-translations.ts` — all prompts in EN/RO/PL/BG

**Approach:** Add `language` column to `message_threads` (migration). Detect language from first message using Claude (single token call). Store on thread. Use throughout.

---

### 🟡 Priority 4: Timesheet PDF Export (S27)
**What it is:** PDF download of a single timesheet — operative name, week, shifts table, total hours/days/pay, approval signature.
**Key files:** `src/app/api/timesheets/[id]/pdf/route.ts` — generates PDF using `@react-pdf/renderer`
**Package needed:** `npm install @react-pdf/renderer`

---

### 🟢 Priority 5: Short Domain (quick)
**What it is:** Replace TinyURL with a proper short link for operative document upload URLs.
Currently: `src/lib/short-link.ts` uses TinyURL API.
Fix: Use the existing `short_links` table + `/r/[code]` redirect route (already built in S30/S31).
**Change:** Update `createShortLink()` in `src/lib/short-link.ts` to use internal short_links table instead of TinyURL.

---

### 🟢 Priority 6: Liam's Real WhatsApp Number
**What it is:** `LIAM_WHATSAPP_NUMBER` env var is currently Oliver's mobile (+447742201349).
**Fix:** Update Vercel env var to Liam's real number when provided.

---

### 🟢 Priority 7: QA Pass (S29)
- BST timezone edge cases in compliance cron and WTD
- Mobile responsiveness polish
- Test Sophie E2E flow (wipe DB → WhatsApp → upload → verify)
- Race condition on allocation offer accept

---

## Known Gotchas (carry forward)
- auth_user_id vs id — ALWAYS use auth_user_id to link public.users to Supabase auth
- allocations.status column = 'status' not 'allocation_status'
- allocations.agreed_day_rate not 'day_rate'
- FK joins MUST use explicit FK name: table!fk_name(cols)
- Next.js 15 params/searchParams must be await-ed
- CREATE OR REPLACE cannot change function return type — DROP first
- ALTER TYPE ADD VALUE cannot run in same transaction as other DDL
- Middleware matcher must exclude static file extensions (already fixed)
- Supabase migrations via SQL editor only — no supabase CLI execute

## Architecture Reminders
- `createServiceClient()` — bypasses RLS, use for crons/admin routes
- `createClient()` — respects RLS, use for user-facing routes
- Sophie state machine: `src/lib/whatsapp/sophie-handler.ts`
- WhatsApp router: `src/lib/whatsapp/handler.ts`
- Twilio webhook: `src/app/api/webhooks/twilio/route.ts`
- Supabase: ybfhkcvrzbgzrayjskfp (Frankfurt)
- Org ID: 00000000-0000-0000-0000-000000000001
- Live URL: https://aztec-landscapes-bos.vercel.app
